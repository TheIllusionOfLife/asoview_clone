# Argo CD Application for the search-service stack. Since the Vertex AI
# Search migration this reconciles only the search-service Deployment +
# Service + KSA + NetworkPolicy — no in-cluster search backend.
#
# Reconciles infra/k8s/search/ into the `search` namespace on the
# asoview-clone-dev cluster. With prune: true the retired OpenSearch
# StatefulSet, headless Service, and ServiceAccount get removed from
# the cluster on the next Argo sync after this change lands.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: search
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: asoview-clone-dev
spec:
  project: default
  source:
    repoURL: https://github.com/TheIllusionOfLife/asoview_clone.git
    targetRevision: main
    path: infra/k8s/search/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: search
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
