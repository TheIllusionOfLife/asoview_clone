apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: gateway
  namespace: edge
spec:
  podSelector:
    matchLabels:
      app: gateway
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - {}
  egress:
    - {}
