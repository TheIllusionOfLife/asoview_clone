node_modules/
.DS_Store

# Root .gitignore has a global `out/` rule; re-include this tool's out/
# so the demo artifacts can ship with the repo.
!out/

# But re-ignore the regenerable pieces inside it.
out/frames/
out/shots.inline.json

# Remotion bundler copy of screenshots (regenerated by capture)
remotion/public/
